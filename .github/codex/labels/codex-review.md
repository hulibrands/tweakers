Review this PR and respond with a concise Markdown comment.

Start with a 1-2 sentence summary of the change. Then provide a normal code
review focused on correctness, compatibility with ShadGPT, user-visible
behavior, and missing coverage. Prefer concrete findings over exhaustive
commentary.

{CODEX_ACTION_GITHUB_EVENT_PATH} contains the JSON that triggered this GitHub
workflow. It contains the `base` and `head` refs that define this PR. Both refs
are available locally.
