# Mode Switcher

Codex++ tweak that adds a Mode section to the Config page and toggles between vanilla Codex and Codex++.

The tweak id is `co.thomashulihan.mode-switcher`. It belongs to the shared `hulibrands/tweakers` repository and is meant to be linked into Codex++ from this directory.
