# Mode Switcher

ShadGPT tweak that adds a Mode section to the Config page and toggles between vanilla Codex and ShadGPT.

The tweak id is `co.thomashulihan.mode-switcher`. It belongs to the shared `hulibrands/tweakers` repository and is meant to be linked into ShadGPT from this directory.
